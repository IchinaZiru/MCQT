以下の四択問題に答えてください。
【重要】出力は必ず以下の中から1文字だけ選んでください：
A / B / C / D

絶対に文章や説明は書かないでください。
もしわからない場合でも必ず A〜D の中から最も適切なものを選んでください。

```c

/* @doc @function FUNC_37fef3 @level L7
 * @logical この関数は、2次元配列で表現されたボードを90度時計回りに回転させます。
 * @precise ボードの外周から内側に向かって、各要素を一時変数を用いて4つの位置を交換することで回転を実現します。
 * @unambiguous ボードの各層に対して、4つの位置を順次交換することで90度回転を行います。
 * @exhaustive 関数はボードを直接変更し、戻り値はありません。
 * @troubleshooting 関数内でエラー処理は行っておらず、SIZEが正しいことを前提としています。
 * @contextualizing この関数は、ゲームやパズルなどでボードの状態を回転させる必要がある場面で使用されます。
 * @condensing 2次元配列のボードを90度時計回りに回転させる関数です。
*/

void FUNC_37fef3(uint8_t board[SIZE][SIZE])
{
	uint8_t i, j, n = SIZE;
	uint8_t tmp;
	for (i = 0; i < n / 2; i++)
	{
		for (j = i; j < n - i - 1; j++)
		{
			tmp = board[i][j];
			board[i][j] = board[j][n - i - 1];
			board[j][n - i - 1] = board[n - i - 1][n - j - 1];
			board[n - i - 1][n - j - 1] = board[n - j - 1][i];
			board[n - j - 1][i] = tmp;
		}
	}
}
```

問題: 関数の目的を最も正しく説明している選択肢は？
A. 4×4 の盤面を 90° 時計回りに回転させる関数。
B. 盤面全体をランダムに並び替え、新しいゲームを開始する関数。
C. 指定した1列または1行を上下に反転させる関数。
D. 4×4 の盤面を 90° 反時計回りに回転させる関数。