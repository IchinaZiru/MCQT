以下の四択問題に答えてください。
【重要】出力は必ず以下の中から1文字だけ選んでください：
A / B / C / D

絶対に文章や説明は書かないでください。
もしわからない場合でも必ず A〜D の中から最も適切なものを選んでください。

```c

/* @doc @function FUNC_87a1b0 @level L7
 * @logical この関数は、ゲームボード上のタイルを左方向に移動させるための処理を行います。
 * @precise ボードを90度回転させてから上方向にタイルを移動し、その後ボードを元の向きに戻すために3回回転させます。
 * @unambiguous ボードを左に移動するために、まず90度回転して上方向に移動し、3回回転して元の向きに戻します。
 * @exhaustive 関数はタイルの移動が成功したかどうかを示すブール値を返しますが、エラー処理は実装されていません。
 * @troubleshooting エラーや例外処理は含まれていないため、入力の検証やエラー処理は別途実装する必要があります。
 * @contextualizing この関数は、2048のようなタイルを動かすゲームで使用されることを想定しています。
 * @condensing ボードを左に移動するために回転と上方向移動を行い、成功をブール値で返します。
*/

bool FUNC_87a1b0(uint8_t board[SIZE][SIZE], uint32_t *score)
{
	bool success;
	rotateBoard(board);
	success = moveUp(board, score);
	rotateBoard(board);
	rotateBoard(board);
	rotateBoard(board);
	return success;
}
```

問題: 関数の目的を最も正しく説明している選択肢は？
A. 盤面をまず 270° 時計回転させて横方向（右方向）にスライドを行い、その結果を 90° 回転させて元に戻す関数。
B. 盤面のすべてのタイルを左に詰め、同じ数値が隣接していれば合体させる関数（回転処理は行わない）。
C. 盤面をまず 90° 時計回転させて縦方向（上方向）にスライドを行い、その結果をさらに 270° 回転させて元の向きに戻す関数。
D. 現在のスコアに応じて盤面全体の配色テーマを変更する関数。