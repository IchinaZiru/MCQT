以下の四択問題に答えてください。
【重要】出力は必ず以下の中から1文字だけ選んでください：
A / B / C / D

絶対に文章や説明は書かないでください。
もしわからない場合でも必ず A〜D の中から最も適切なものを選んでください。

```c

/* @doc @function FUNC_2d8251 @level L7
 * @logical この関数はゲームボードを右に移動させるための処理を行います。
 * @precise ボードを3回回転させた後、上方向への移動処理を行い、最後にもう一度ボードを回転させます。
 * @unambiguous ボードを3回回転させてから上方向に移動し、再度1回回転させることで右方向への移動を実現します。
 * @exhaustive 関数は右方向への移動が成功したかどうかを示すブール値を返します。
 * @troubleshooting エラー処理は関数内に含まれておらず、呼び出し元での対処が必要です。
 * @contextualizing この関数は、2048のようなゲームで右方向への移動を実現するために使用されます。
 * @condensing ボードを右に移動させるために3回回転し、上方向に移動してから再度回転します。
*/

bool FUNC_2d8251(uint8_t board[SIZE][SIZE], uint32_t *score)
{
	bool success;
	rotateBoard(board);
	rotateBoard(board);
	rotateBoard(board);
	success = moveUp(board, score);
	rotateBoard(board);
	return success;
}
```

問題: 関数の目的を最も正しく説明している選択肢は？
A. 盤面をまず 180° 時計回転させて縦方向（上方向）にスライドを行い、その結果をさらに 180° 回転させて元の向きに戻す関数。
B. 盤面をまず 270° 時計回転させて縦方向（上方向）にスライドを行い、その結果をさらに 90° 回転させて元の向きに戻す関数。
C. 盤面全体を右に1マスずつシフトして、新しいタイルを左端に追加する関数。
D. ユーザー入力に応じて、盤面全体のスコアを更新しリスタートを行う関数。