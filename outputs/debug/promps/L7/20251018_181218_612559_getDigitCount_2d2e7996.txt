以下の四択問題に答えてください。
【重要】出力は必ず以下の中から1文字だけ選んでください：
A / B / C / D

絶対に文章や説明は書かないでください。
もしわからない場合でも必ず A〜D の中から最も適切なものを選んでください。

```c

/* @doc @function FUNC_8eef1b @level L7
 * @logical この関数は、与えられた整数の桁数を計算して返します。
 * @precise 入力された整数を10で割り続け、割り切れるまでの回数をカウントします。
 * @unambiguous 整数を10で割る操作を繰り返し、割り切れるまでの繰り返し回数を桁数として返します。
 * @exhaustive 関数は常に0以上の整数の桁数を返し、負の数や例外は考慮されていません。
 * @troubleshooting この関数はエラーや例外処理を行わないため、入力が32ビットの非負整数であることを前提としています。
 * @contextualizing この関数は、数値の桁数が必要な場面で使用されます。
 * @condensing 与えられた整数の桁数を計算して返す関数です。
*/

uint8_t FUNC_8eef1b(uint32_t number)
{
	uint8_t count = 0;
	do
	{
		number /= 10;
		count += 1;
	} while (number);
	return count;
}
```

問題: 関数の目的を最も正しく説明している選択肢は？
A. 盤面全体の配色テーマをスコアに応じて自動切替するためのしきい値を返す。
B. 与えられた数値を**2のべきの指数（log₂）**に変換して、その指数を返す。
C. 各タイルの数字の桁数を数える。
D. 与えられた数値の桁数を数えるが、数値が0の場合は0桁として返す。