以下の四択問題に答えてください。
【重要】出力は必ず以下の中から1文字だけ選んでください：
A / B / C / D

絶対に文章や説明は書かないでください。
もしわからない場合でも必ず A〜D の中から最も適切なものを選んでください。

```c

void FUNC_87ce1e(uint8_t board[SIZE][SIZE])
{
	uint8_t x, y;
	for (x = 0; x < SIZE; x++)
	{
		for (y = 0; y < SIZE; y++)
		{
			board[x][y] = 0;
		}
	}
	addRandom(board);
	addRandom(board);
}
```

問題: 関数の目的を最も正しく説明している選択肢は？
A. 盤面上の空きマスを数えて次の手が可能かどうかを判定する関数。
B. 盤面全体を180°回転させてタイルの位置を入れ替える関数。
C. 盤面をリセットして最初の2枚のタイルを出し、ゲームをスタート可能な状態にする関数。
D. 現在の盤面をバックアップファイルとして保存する関数。