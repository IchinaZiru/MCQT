以下の四択問題に答えてください。
【重要】出力は必ず以下の中から1文字だけ選んでください：
A / B / C / D

絶対に文章や説明は書かないでください。
もしわからない場合でも必ず A〜D の中から最も適切なものを選んでください。

```c

bool FUNC_87a1b0(uint8_t board[SIZE][SIZE], uint32_t *score)
{
	bool success;
	rotateBoard(board);
	success = moveUp(board, score);
	rotateBoard(board);
	rotateBoard(board);
	rotateBoard(board);
	return success;
}
```

問題: 関数の目的を最も正しく説明している選択肢は？
A. 盤面をまず 270° 時計回転させて横方向（右方向）にスライドを行い、その結果を 90° 回転させて元に戻す関数。
B. 盤面のすべてのタイルを左に詰め、同じ数値が隣接していれば合体させる関数（回転処理は行わない）。
C. 盤面をまず 90° 時計回転させて縦方向（上方向）にスライドを行い、その結果をさらに 270° 回転させて元の向きに戻す関数。
D. 現在のスコアに応じて盤面全体の配色テーマを変更する関数。